# Leave blank for local development (Vite proxies /api to Wrangler on 127.0.0.1:8787).
# Set this to the deployed We-Rise Worker origin for production, for example:
# VITE_API_URL=https://werise-api.example.workers.dev
VITE_API_URL=
